---
# - name: Get ServerDesity SystemKey
#   command: grep -o -P '(?<=systemKey\":\")[a-zA-Z0-9]*' {{ serverdensity_agent_config }}
#   register: serverdensity_system_key
#   become: "{{ serverdensity_use_sudo }}"
#
# - name: Update System Information
#   command: >
#           curl -iq \
#                    -d '{
#                       "tags" : [ {{ '"' + serverdensity_tag|join('", "') + '"' }} ],
#                       "displayName" : "{{ serverdensity_displayName }}",
#                       "allowPublicKeyAuthentication" : "{{ serverdensity_allowPublicKeyAuthentication }}",
#                       "allowSshPasswordAuthentication" : "{{ serverdensity_allowSshPasswordAuthentication }}",
#                       "allowSshRootLogin" : "{{ serverdensity_allowSshRootLogin }}",
#                       "allowMultiFactorAuthentication" : "{{ serverdensity_allowMultiFactorAuthentication }}"
#                     }' \
#                    -X 'PUT' \
#                    -H 'Content-Type: application/json' \
#                    -H 'Accept: application/json' \
#                    -H 'x-api-key: {{ serverdensity_api_key }}' \
#                    "{{ serverdensity_api_url }}/systems/{{ serverdensity_system_key.stdout }}"
#   delegate_to: localhost
#   #no_log: True
#   when: serverdensity_system_key is defined
#   become: "{{ serverdensity_use_sudo }}"

# It would be more elegant to use the URI: syntax..but can't see how to pass the data in the correct format needed to the RESTAPI
# can't understand if it has to go { "body:" { "comtent: { "data"}"}}... or somewhere else
# - name: Update System Information
#   uri:
#     url: "{{ serverdensity_api_url }}/systems/{{ serverdensity_system_key.stdout }}"
#     headers:
#       Content-Type: 'application/json'
#       Accept:       'application/json'
#       x-api-key:    "{{ serverdensity_api_key }}"
#     body_format:  json
#     body:
#       content:
#         tags: ["xxx","yyy","zzz"]
#         displayName: "{{ serverdensity_displayName }}"
#     method: PUT
#     follow_redirects: all
#     return_content: yes
#   delegate_to: localhost
#   register: http_outcome

...
